```{=html}
<div class="talks">
  <% for (const talk of items) { %>
  <div class="talk-entry">
    <div class="metadata">          
      <p class="date"><%= talk.date %></p>
    </div>
    <div class="body">
      <p class="title"><a href="<%= talk.path %>"><%= talk.title %></a></p>
      <p class="event-location"><%= talk.event_short %> | <%= talk.location %></p>
      <% if (talk.links) { %>
        <ul class="talk-links">
          <% for (const link of talk.links) { %>
            <li class="talk-link btn btn-outline-primary">
              <a href="<%= link.url %>">
                <i class="fa-solid fa-<%= link.faicon %>" aria-label="<%= link.faicon %>"></i>
                <%= link.text %>
              </a>
            </li>
          <% } %>
        </ul>
      <% } %>
    </div>
  </div>
  <% } %>
</div>
```
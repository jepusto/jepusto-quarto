```{=html}
<div class="publications">
  <ul class="list">
  <% for (const item of items) { %>
  <li class="paper" <%= metadataAttrs(item) %>>
    <div class="metadata">          
      <p class="listing-date"><%= item.date %></p>
    </div>
    <div class="body">
      <p class="listing-title">
        <a href="<%= item.path %>"><%= item.title %></a>
      </p>
      <% if (item.author) { %>
      <p class="listing-author"><%= item.author %></p>
      <% } %>
      <p class="citation">
        <% if (item.citation) { %>
        <% if (item.citation.type == "article-journal") { %>
          <em class="listing-publication"><%= item.citation['container-title'] %>,</em>
          <% if (item.citation.volume) { %><em><%= item.citation.volume %></em><% if (item.citation.issue) { %>(<%= item.citation.issue %>),<% } else {%>,<% } %><% } %>
          <% if (item.citation.page) { %>
            <%= item.citation.page %>.
          <% } %>
        <% } else if (item.citation.type == "report") {%>
          <em class="listing-publication"><%= item.citation['container-title'] %>,</em>
          <% if (item.citation.publisher) { %><%= item.citation.publisher %><% if (item.citation['publisher-place']) { %>, <%= item.citation['publisher-place'] %><% } %>.<% } %>
        <% } else if (item.citation.type == "chapter") {%>
          <% if (item.citation['container-title']) { %>In <em><%= item.citation['container-title'] %></em>.<% } %> <% if (item.citation['publisher-place']) {%><%= item.citation['publisher-place'] %>: <% } %><% if (item.citation.publisher) {%><%= item.citation.publisher %>.<% } %>
        <% } else if (item.citation.type == "thesis") {%>
          <% if (item.citation.genre) { %><%= item.citation.genre %><% } %><% if (item.citation.publisher) {%>, <%= item.citation.publisher %>.<% } %>
        <% } %>
      <% } %>
      </p>
      <% if (item.links) { %>
        <ul class="publication-links">
          <% for (const link of item.links) { %>
            <li class="publication-link btn btn-outline-primary">
              <a href="<%= link.url %>">
                <i class="bi bi-<%= link.icon %>" aria-label="<%= link.icon %>"></i>
                <%= link.text %>
              </a>
            </li>
          <% } %>
        </ul>
      <% } %>
    </div>
  </li>
  <% } %>
  </ul>
</div>
```